[package]
name = "qcy-device"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Device profiles and the four-dimensional capability truth model (Rust mirror of issue #3)"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
